<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.my">
	<select id="location" resultType="java.util.HashMap">
		select * from location 
	</select>
	<select id="industry" resultType="java.util.HashMap">
		select * from industry 
	</select>
	<!-- <insert id="addinfos" parameterType="java.util.HashMap" >
		insert into MEMBER_INFO where area=#{area}, STNDD_BIG_GB=#{STNDD_BIG_GB}, salary_min=#{salary_min}, salary_max=#{salary_max}
	</insert> -->
</mapper>